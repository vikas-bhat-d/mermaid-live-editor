// Analytics disabled - offline-first application
// No tracking is performed to ensure privacy and offline functionality

export const initAnalytics = async (): Promise<void> => {
  // No-op: analytics disabled
};

export const logEvent = (): void => {
  // No-op: event logging disabled
};

export const logMermaidChartClick = (): void => {
  // No-op: event logging disabled
};
